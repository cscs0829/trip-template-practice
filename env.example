# 데이터베이스 연결 URL (Render PostgreSQL 또는 로컬 개발용)
DATABASE_URL=postgresql://username:password@hostname:port/database_name

# 환경 설정
NODE_ENV=development
PORT=3000

# Render에서 자동으로 설정되는 환경변수들
# DATABASE_URL은 Render에서 PostgreSQL 서비스 연결시 자동 생성됩니다
