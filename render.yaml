services:
  - type: pserv
    name: trippage-db
    plan: free
    
  - type: web
    name: trippage-web
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
      - key: DATABASE_URL
        fromDatabase:
          name: trippage-db
          property: connectionString
